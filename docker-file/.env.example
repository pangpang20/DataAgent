# ============================================================================
# DataAgent Docker Compose 环境变量配置示例
# ============================================================================
# 使用说明：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 修改下面的配置值
# 3. 运行 docker-compose up -d
# ============================================================================

# AI 服务 API Key（可选）
# 阿里云 DashScope API Key，用于访问通义千问等大模型
# 获取地址：https://dashscope.console.aliyun.com/apiKey
# 注：
#   - 未配置时：系统正常启动，可在前端界面配置模型后使用 NL2SQL
#   - 已配置时：作为默认模型配置，无需前端重复配置
AI_DASHSCOPE_API_KEY=

# 可选：数据卷存储路径
# 默认使用当前目录，可修改为指定路径
# DOCKER_VOLUME_DIRECTORY=/data/dataagent
